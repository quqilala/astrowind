# OUTGOPRO

Read DESIGN.md before changing copy, colors, typography or page structure. It records the user-confirmed business scope and visual direction.

Business copy lives in src/data/services.ts. Public marketing pages use src/layouts/OutgoLayout.astro. Avoid editing unused AstroWind template widgets as a way to change the current website.

The production Pages project is `outgopro`, connected to the `main` branch of `quqilala/astrowind`. Use preview deployments for browser checks before publishing. Keep credentials out of repository files.
