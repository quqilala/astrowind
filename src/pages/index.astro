---
import Layout from '~/layouts/PageLayout.astro';
import Image from '~/components/common/Image.astro';

import heroImage from '~/assets/images/hero-image.png';

const metadata = {
  title: 'OUTGOPRO — 出海增长与合规导航',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <!-- ==================== Hero 区 ==================== -->
  <section class="relative overflow-hidden bg-gradient-to-br from-slate-50 via-white to-blue-50/40 dark:from-slate-950 dark:via-slate-900 dark:to-slate-950">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 pt-8 pb-16 md:pt-12 md:pb-24">
      <div class="grid md:grid-cols-2 gap-10 md:gap-14 items-center">
        <!-- 左侧文案 -->
        <div>
          <div class="inline-flex items-center gap-1.5 text-xs tracking-widest font-bold uppercase rounded-full px-3 py-1.5 bg-blue-100 text-blue-700 dark:bg-blue-900/40 dark:text-blue-200 mb-5">
            🚀 OUTGOPRO
          </div>

          <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-[3.25rem] font-extrabold tracking-tight leading-[1.15] mb-5 text-slate-900 dark:text-white" data-i18n="heroTitle">
            别卷了，去赚全世界的钱。
          </h1>

          <p class="text-base sm:text-lg text-slate-500 dark:text-slate-400 mb-8 leading-relaxed max-w-lg" data-i18n="heroDesc">
            AI 提效、海外开公司报税、搭网络、做跨境电商——我们把踩过的坑铺成路，你直接走就好。
          </p>

          <div class="flex flex-wrap gap-3">
            <a href="/ai-tools" class="group inline-flex items-center gap-2 px-6 py-3 rounded-full bg-blue-600 text-white font-semibold hover:bg-blue-700 transition-colors" data-i18n="heroBtn1">
              开始探索 →
            </a>
            <a href="/about-us" class="inline-flex items-center gap-2 px-6 py-3 rounded-full border border-slate-300 dark:border-slate-600 font-semibold text-slate-700 dark:text-slate-200 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors" data-i18n="heroBtn2">
              聊聊合作
            </a>
          </div>
        </div>

        <!-- 右侧配图 -->
        <div class="relative">
          <div class="absolute -inset-4 bg-gradient-to-tr from-blue-400/20 to-purple-400/20 dark:from-blue-500/10 dark:to-purple-500/10 rounded-3xl blur-2xl"></div>
          <Image src={heroImage} alt="Outgopro Hero" class="relative w-full rounded-2xl shadow-2xl" />
        </div>
      </div>
    </div>
  </section>

  <!-- ==================== 业务板块 ==================== -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 py-16 md:py-24">
    <!-- 板块标题 -->
    <div class="text-center mb-12">
      <h2 class="text-2xl md:text-3xl font-bold text-slate-900 dark:text-white mb-3" data-i18n="sectionTitle">我们能帮你做什么？</h2>
      <p class="text-slate-500 dark:text-slate-400 max-w-xl mx-auto" data-i18n="sectionDesc">五个核心板块，覆盖出海路上最关键的环节。</p>
    </div>

    <!-- 卡片网格 -->
    <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
      <!-- AI 工具 -->
      <a href="/ai-tools" class="card-link group">
        <span class="text-3xl mb-3 block">🤖</span>
        <h3 class="text-lg font-bold mb-1.5 text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors" data-i18n="aiTitle">AI 工具及工作流</h3>
        <p class="text-sm text-slate-500 dark:text-slate-400 leading-relaxed" data-i18n="aiDesc">让 AI 替你干活，从内容生产到客户触达，用更少的人做更多的事。</p>
      </a>

      <!-- 海外设立报税 -->
      <a href="/global-tax" class="card-link group">
        <span class="text-3xl mb-3 block">🏦</span>
        <h3 class="text-lg font-bold mb-1.5 text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors" data-i18n="taxTitle">海外公司 / 账户设立报税</h3>
        <p class="text-sm text-slate-500 dark:text-slate-400 leading-relaxed" data-i18n="taxDesc">开公司、开户、报税一条龙，别让合规问题在最该冲刺的时候绊倒你。</p>
      </a>

      <!-- 网络技术 -->
      <a href="/network-tech" class="card-link group">
        <span class="text-3xl mb-3 block">🌐</span>
        <h3 class="text-lg font-bold mb-1.5 text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors" data-i18n="netTitle">网络技术</h3>
        <p class="text-sm text-slate-500 dark:text-slate-400 leading-relaxed" data-i18n="netDesc">稳定的网络是出海的命根子。架构、安全、加速，一个都不能少。</p>
      </a>

      <!-- 跨境电商 -->
      <a href="/cross-border" class="card-link group">
        <span class="text-3xl mb-3 block">🛒</span>
        <h3 class="text-lg font-bold mb-1.5 text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors" data-i18n="ecomTitle">跨境电商</h3>
        <p class="text-sm text-slate-500 dark:text-slate-400 leading-relaxed" data-i18n="ecomDesc">选品、流量、履约——帮你找到能持续赚钱的增长路径，不靠玄学。</p>
      </a>

      <!-- 关于我们 -->
      <a href="/about-us" class="card-link group sm:col-span-2 lg:col-span-2">
        <span class="text-3xl mb-3 block">✌️</span>
        <h3 class="text-lg font-bold mb-1.5 text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors" data-i18n="aboutTitle">关于我们</h3>
        <p class="text-sm text-slate-500 dark:text-slate-400 leading-relaxed" data-i18n="aboutDesc">一群在技术、财税和商业增长交叉路口蹲了很久的人。方法论靠谱，不画饼。</p>
      </a>
    </div>
  </section>

  <!-- ==================== 底部 CTA ==================== -->
  <section class="bg-slate-50 dark:bg-slate-900/50">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 py-16 md:py-20 text-center">
      <h2 class="text-2xl md:text-3xl font-bold text-slate-900 dark:text-white mb-4" data-i18n="ctaTitle">准备好出海了吗？🌊</h2>
      <p class="text-slate-500 dark:text-slate-400 mb-8 max-w-lg mx-auto" data-i18n="ctaDesc">不管你是刚起步还是已经在路上，我们都可以聊聊。</p>
      <a href="/about-us" class="inline-flex items-center gap-2 px-7 py-3.5 rounded-full bg-blue-600 text-white font-semibold hover:bg-blue-700 transition-colors" data-i18n="ctaBtn">联系我们 →</a>
    </div>
  </section>

  <!-- ==================== 双语切换逻辑 ==================== -->
  <script is:inline>
    const dict = {
      zh: {
        heroTitle: '别卷了，去赚全世界的钱。',
        heroDesc: 'AI 提效、海外开公司报税、搭网络、做跨境电商——我们把踩过的坑铺成路，你直接走就好。',
        heroBtn1: '开始探索 →',
        heroBtn2: '聊聊合作',
        sectionTitle: '我们能帮你做什么？',
        sectionDesc: '五个核心板块，覆盖出海路上最关键的环节。',
        aiTitle: '🤖 AI 工具及工作流',
        aiDesc: '让 AI 替你干活，从内容生产到客户触达，用更少的人做更多的事。',
        taxTitle: '🏦 海外公司 / 账户设立报税',
        taxDesc: '开公司、开户、报税一条龙，别让合规问题在最该冲刺的时候绊倒你。',
        netTitle: '🌐 网络技术',
        netDesc: '稳定的网络是出海的命根子。架构、安全、加速，一个都不能少。',
        ecomTitle: '🛒 跨境电商',
        ecomDesc: '选品、流量、履约——帮你找到能持续赚钱的增长路径，不靠玄学。',
        aboutTitle: '✌️ 关于我们',
        aboutDesc: '一群在技术、财税和商业增长交叉路口蹲了很久的人。方法论靠谱，不画饼。',
        ctaTitle: '准备好出海了吗？🌊',
        ctaDesc: '不管你是刚起步还是已经在路上，我们都可以聊聊。',
        ctaBtn: '联系我们 →',
      },
      en: {
        heroTitle: "Stop grinding. Go make money globally.",
        heroDesc: "AI productivity, overseas company setup & tax, networking, cross-border e-commerce — we've paved the road with lessons learned. Just walk it.",
        heroBtn1: 'Get Started →',
        heroBtn2: "Let's Talk",
        sectionTitle: 'What can we do for you?',
        sectionDesc: 'Five core areas covering the most critical steps of going global.',
        aiTitle: '🤖 AI Tools & Workflows',
        aiDesc: 'Let AI do the heavy lifting — from content creation to customer outreach, do more with less.',
        taxTitle: '🏦 Overseas Entity & Tax Filing',
        taxDesc: "Company setup, bank accounts, tax compliance — don't let red tape trip you up when you should be sprinting.",
        netTitle: '🌐 Network Technology',
        netDesc: "Reliable networking is your lifeline abroad. Architecture, security, acceleration — you need them all.",
        ecomTitle: '🛒 Cross-border E-commerce',
        ecomDesc: 'Product selection, traffic, fulfillment — find a growth path that actually makes money, no guesswork.',
        aboutTitle: '✌️ About Us',
        aboutDesc: "A crew that's been at the crossroads of tech, tax, and growth for a while. Solid playbooks, no BS.",
        ctaTitle: 'Ready to go global? 🌊',
        ctaDesc: "Whether you're just starting out or already on the road, let's chat.",
        ctaBtn: 'Contact Us →',
      },
    };

    // ===== 导航栏双语联动 =====
    const navDict = {
      zh: {
        '/ai-tools': 'AI工具及工作流',
        '/global-tax': '海外公司/账户设立报税',
        '/network-tech': '网络技术',
        '/cross-border': '跨境电商',
        '/about-us': '关于我们',
        '_action': '联系咨询',
      },
      en: {
        '/ai-tools': 'AI Tools',
        '/global-tax': 'Global Setup & Tax',
        '/network-tech': 'Network Tech',
        '/cross-border': 'Cross-border E-com',
        '/about-us': 'About Us',
        '_action': 'Contact',
      },
    };

    let current = localStorage.getItem('outgopro-lang') || 'zh';

    const normalizePath = (href) => {
      try { return new URL(href, location.origin).pathname.replace(/\/$/, '') || '/'; }
      catch { return href; }
    };

    // 在 header 右侧注入语言切换按钮（不占首页空间）
    const injectLangBtn = () => {
      if (document.getElementById('nav-lang-toggle')) return;
      // 找到 header 右侧的 actions 区域
      const headerActions = document.querySelector('header .items-center.flex.justify-between');
      if (!headerActions) return;
      const btn = document.createElement('button');
      btn.id = 'nav-lang-toggle';
      btn.className = 'ml-2 px-3 py-1.5 text-xs font-semibold rounded-lg border border-slate-300 dark:border-slate-600 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors';
      btn.type = 'button';
      btn.textContent = current === 'zh' ? '🌐 EN' : '🌐 中文';
      btn.addEventListener('click', () => {
        current = current === 'zh' ? 'en' : 'zh';
        renderAll();
      });
      headerActions.querySelector('.flex')?.appendChild(btn);
    };

    const renderNav = () => {
      document.querySelectorAll('header nav a[href]').forEach((a) => {
        const path = normalizePath(a.getAttribute('href') || '');
        if (navDict[current][path]) a.textContent = navDict[current][path];
      });
      // 更新 action 按钮文案
      const actionBtns = document.querySelectorAll('header .ml-4 a, header .ml-4 button');
      actionBtns.forEach(b => { if (navDict[current]['_action']) b.textContent = navDict[current]['_action']; });
      // 更新语言按钮自身
      const langBtn = document.getElementById('nav-lang-toggle');
      if (langBtn) langBtn.textContent = current === 'zh' ? '🌐 EN' : '🌐 中文';
    };

    const renderPage = () => {
      document.querySelectorAll('[data-i18n]').forEach((el) => {
        const key = el.getAttribute('data-i18n');
        if (key && dict[current]?.[key]) {
          // 保留 emoji 前的 span 子元素（卡片 emoji 是独立 span，不受影响）
          el.textContent = dict[current][key];
        }
      });
      document.documentElement.lang = current === 'zh' ? 'zh-CN' : 'en';
      localStorage.setItem('outgopro-lang', current);
    };

    const renderAll = () => { renderPage(); renderNav(); };

    // 初始化
    injectLangBtn();
    renderAll();

    // 兼容 Astro View Transitions：页面切换后重新注入
    document.addEventListener('astro:after-swap', () => { injectLangBtn(); renderAll(); });
  </script>

  <style>
    .card-link {
      display: block;
      border-radius: 1rem;
      border: 1px solid rgba(148, 163, 184, 0.25);
      padding: 1.5rem;
      background: rgba(255, 255, 255, 0.9);
      transition: all 0.25s ease;
    }

    :global(.dark) .card-link {
      background: rgba(15, 23, 42, 0.7);
      border-color: rgba(100, 116, 139, 0.4);
    }

    .card-link:hover {
      transform: translateY(-4px);
      border-color: rgba(59, 130, 246, 0.5);
      box-shadow: 0 16px 40px -8px rgba(2, 6, 23, 0.1);
    }

    :global(.dark) .card-link:hover {
      box-shadow: 0 16px 40px -8px rgba(0, 0, 0, 0.4);
      border-color: rgba(96, 165, 250, 0.5);
    }
  </style>
</Layout>
